{
  "name": "opencode-kv-warmup",
  "version": "0.2.0",
  "description": "Pre-warm llama-server KV cache on session start to reduce TTFT",
  "type": "module",
  "main": "plugin.js",
  "private": true,
  "scripts": {
    "test": "node --test test/",
    "lint": "node -c plugin.js",
    "check": "npm run lint && npm run test"
  },
  "license": "MIT",
  "author": "Juan Manuel Parrilla Madrid",
  "engines": {
    "node": ">=18"
  }
}
